<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppThemeLight">
        <item name="android:buttonStyle">@style/ButtonStyle</item>

        <item name="hintTextAppearance">@style/HintStyle</item>
        <item name="errorTextAppearance">@style/ErrorStyle</item>
        <item name="counterTextAppearance">@style/CounterStyle</item>
        <item name="counterOverflowTextAppearance">@style/CounterOverflowStyle</item>
    </style>

    <!-- Light application theme. -->
    <style name="AppThemeLight" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/secondary</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorPrimaryInverse">@color/primary_text_inverse</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:navigationBarColor">@color/primary_dark</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/secondary</item>
        <item name="android:textColorSecondaryInverse">@color/secondary_text_inverse</item>
    </style>

    <!-- Dark application theme. -->
    <style name="AppThemeDark" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/secondary</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorPrimaryInverse">@color/primary_text_inverse</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:navigationBarColor">@color/primary_dark</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/secondary</item>
        <item name="android:textColorSecondaryInverse">@color/secondary_text_inverse</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/primary</item>
        <item name="android:textColor">@color/primary_text_inverse</item>
    </style>

    <style name="HintStyle">
        <item name="android:textColor">@color/secondary_text</item>
    </style>

    <style name="ErrorStyle">
        <!--<item name="android:textColor">@color/error</item>-->
    </style>

    <style name="CounterStyle">
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="CounterOverflowStyle">
        <!--<item name="android:textColor">@color/primary</item>-->
    </style>

</resources>
